# Entrada - inseir a base e a altura do retângulo
base = float(input("Digite o comprimento da base do retânfulo:"))
altura = float(input("Digite o comprimento da altura do retângulo:"))

# Calcular a área do retângulo
area = base * altura

# Exibir o resultado
print(f"A área do retângulo é igual a {area}")